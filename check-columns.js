const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.rpc('query_db', { query_text: "SELECT column_name FROM information_schema.columns WHERE table_name = 'email_templates'" }); console.log(data); } run();
